<div class="video-container" id="video-container">
  <iframe data-src="https://player.vimeo.com/video/802387040?background=1&amp;wmode=opaque" frameborder="0" allow="autoplay; fullscreen" allowfullscreen id="fitvid0" title="Copenhagen Light Festival"></iframe>
</div>
</div>

<style> 

 .video-container {
        width: 100%;
        position: relative;
        padding-bottom: 48.25%; /* 16:9 aspect ratio */
        height: 0;
        overflow: hidden;
        display: flex;
        justify-content: center;
        align-items: center;
        margin-top: 12rem;
    }

    .video-container iframe {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

    @media screen and (max-width: 800px) {
        /* Adjust the margin-top for smaller screens */
        .video-container {
            margin-top: 3rem; /* Change this value as needed */
        }
    }

</style>
<script>
  // Add your lazy loading script here
  const iframe = document.querySelector('iframe[data-src]');

  if (iframe) {
    const observer = new IntersectionObserver((entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          iframe.setAttribute('src', iframe.getAttribute('data-src'));
          observer.unobserve(iframe);
        }
      });
    });

    observer.observe(iframe);
  }
</script>
